//  Create a system to calculate popcorn prices based ont the size customer asked for:

let size ='L';

if(size === 'XL'){
    console.log(`Your price is 200.because size is ${size}`);
    
}
else if (size === 'L') {
    console.log(`your price is 250. because size is  ${size}`);
    
    
}
else if(size ==='M'){
    console.log(`your price is 100. because size is ${size}`);
    
}
else {
    console.log(`your price is 50. because size is ${size}`);
    
}